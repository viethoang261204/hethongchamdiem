services:
  - type: web
    name: hethongchamdiem
    env: node
    buildCommand: cd client && npm install && npm run build && cd ../server && npm install
    startCommand: node server/index.cjs
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
